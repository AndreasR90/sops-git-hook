import sys

config_file = ".secrets.yaml"


def main():
    # configuration = yaml.safe_load(config_file)
    # secrets = configuration["secrets"]
    # for secret in secrets:
    #     encrypt_secret(secret)
    sys.exit(1)


if __name__ == "__main__":
    print("IN MAIN FUNCTOIN")
